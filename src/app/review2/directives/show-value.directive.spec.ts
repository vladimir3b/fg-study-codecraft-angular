import { ShowValueDirective } from './show-value.directive';

describe('ShowValueDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowValueDirective();
    expect(directive).toBeTruthy();
  });
});
