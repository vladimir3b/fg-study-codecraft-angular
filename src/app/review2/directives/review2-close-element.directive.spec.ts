import { Review2CloseElementDirective } from './review2-close-element.directive';

describe('Review2CloseElementDirective', () => {
  it('should create an instance', () => {
    const directive = new Review2CloseElementDirective();
    expect(directive).toBeTruthy();
  });
});
