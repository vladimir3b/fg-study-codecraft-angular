import { RepeatElementDirective } from './repeat-element.directive';

describe('RepeatElementDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatElementDirective();
    expect(directive).toBeTruthy();
  });
});
