import { Chap06ShowOnHoverDirective } from './chap06-show-on-hover.directive';

describe('Chap06ShowOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new Chap06ShowOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
