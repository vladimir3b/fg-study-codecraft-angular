import { Chap06ChangeOnHoverDirective } from './chap06-change-on-hover.directive';

describe('Chap06ChangeOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new Chap06ChangeOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
